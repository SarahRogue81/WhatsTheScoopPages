{% extends 'base.html' %} {% block head_meta %} {% if
articles_page.twitter_username %}
<meta
  name="twitter:creator"
  content="@{{ articles_page.object_list[0].twitter_username }}"
/>
{% elif TWITTER_USERNAME %}
<meta name="twitter:creator" content="@{{ TWITTER_USERNAME }}" />
{% endif %}
<meta
  name="description"
  content="{{ articles_page.summary | striptags | safe }}"
/>
{% endblock %} {% block content %}
<div class="w3-container w3-mobile">
  <div class="w3-panel w3-theme w3-center">
    <div class="w3-container w3-cell">
      <h1>{{ articles_page.object_list[0].title }}</h1>
    </div>
    <div class="w3-container w3-cell w3-cell-middle w3-right">
      by {{ AUTHOR }} on {{ articles_page.object_list[0].date.strftime("%B %d,
      %Y") }}
    </div>
  </div>
</div>

<!-- Single post display -->
<article class="w3-container">
  <div class="w3-panel w3-border w3-padding-16 w3-paper">
    <div class="w3-cell-row w3-mobile">
      <div class="w3-container w3-cell">
        {{ articles_page.object_list[0].content }}
      </div>
    </div>
  </div>
</article>

<!-- Navigation arrows -->
<div class="w3-container w3-center w3-margin-bottom">
  <div class="w3-bar">
    <!-- First page -->
    {% if articles_page.has_previous() %}
    <a
      href="{{ SITEURL }}/{{ page_name }}.html"
      class="w3-bar-item w3-button w3-theme-dark"
    >
      <i class="fa-solid fa-angles-left"></i> First
    </a>
    {% else %}
    <span class="w3-bar-item w3-button w3-disabled">
      <i class="fa-solid fa-angles-left"></i> First
    </span>
    {% endif %}

    <!-- Previous page -->
    {% if articles_page.has_previous() %}
    <a
      href="{{ SITEURL }}/{{ articles_previous_page.url }}"
      class="w3-bar-item w3-button w3-theme-dark"
    >
      <i class="fa-solid fa-angle-left"></i> Previous
    </a>
    {% else %}
    <span class="w3-bar-item w3-button w3-disabled">
      <i class="fa-solid fa-angle-left"></i> Previous
    </span>
    {% endif %}

    <!-- Page indicator -->
    <span class="w3-bar-item w3-button w3-theme-l3">
      {{ articles_page.number }} of {{ articles_paginator.num_pages }}
    </span>

    <!-- Next page -->
    {% if articles_page.has_next() %}
    <a
      href="{{ SITEURL }}/{{ articles_next_page.url }}"
      class="w3-bar-item w3-button w3-theme-dark"
    >
      Next <i class="fa-solid fa-angle-right"></i>
    </a>
    {% else %}
    <span class="w3-bar-item w3-button w3-disabled">
      Next <i class="fa-solid fa-angle-right"></i>
    </span>
    {% endif %}

    <!-- Last page -->
    {% if articles_page.has_next() %}
    <a
      href="{{ SITEURL }}/index{{ articles_paginator.num_pages }}.html"
      class="w3-bar-item w3-button w3-theme-dark"
    >
      Last <i class="fa-solid fa-angles-right"></i>
    </a>
    {% else %}
    <span class="w3-bar-item w3-button w3-disabled">
      Last <i class="fa-solid fa-angles-right"></i>
    </span>
    {% endif %}
  </div>
</div>
{% endblock %}
